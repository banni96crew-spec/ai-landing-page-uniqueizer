#.cursorrules - AI Landing Page Uniqueizer (2026)

<context>
  You are an Expert Senior Fullstack Architect and a senior Python/Next.js developer.
  Your primary objective is to implement the "AI Landing Page Uniqueizer" EXACTLY as specified in the PRD.
  The PRD is the Absolute Single Source of Truth (SSoT). Do not hallucinate external requirements.
</context>

<hierarchy_of_truth>
  CRITICAL RULE: In case of ANY logical contradiction or ambiguity during generation:
  1st Priority: PRD Section 3 (DDL and Database Schema) is the SUPREME truth. The SQLite schema is locked.
  2nd Priority: PRD Section 4 (Functional Requirements and Edge Cases M1-M7).
  3rd Priority: PRD Section 2 (Core Logic & Pipeline).
</hierarchy_of_truth>

<architectural_constraints>
  <constraint id="C1" name="Single_Process_Runtime">
    The FastAPI backend and the background worker MUST run in the SAME OS process.
    Instantiate the worker as an `asyncio.create_task()` inside the FastAPI `@asynccontextmanager` lifespan.
    PROHIBITED TECHNOLOGIES: Redis, Celery, RabbitMQ, ARQ, or any multi-process orchestrators.
  </constraint>
  
  <constraint id="C2" name="Event_Loop_Protection">
    Any CPU-bound or blocking task MUST be offloaded to prevent blocking the event loop.
    - Pillow-SIMD image processing (EXIF stripping, cropping).
    - Numpy array math (noise injection).
    - LightningCSS AST processing.
    - Synchronous zipfile archiving.
    Offload using `loop.run_in_executor()` or `asyncio.to_thread()`. Failure to do so will disconnect WebSockets.
  </constraint>

  <constraint id="C3" name="Anti_Rename">
    DO NOT rename ANY routes, parameters, database columns, HTTP methods, or React component names defined in the PRD unless explicitly marked with the `` tag.
    Example: Do not rename `<JobInputPanel />` to `<UrlForm />`. Do not rename `progress_pct` to `progress`.
  </constraint>
  
  <constraint id="C4" name="Strict_DDL_Compliance">
    The SQLite schema provided in PRD Section 3.2 is final.
    - The `jobs.status` column has ONLY 4 states: 'pending', 'running', 'done', 'failed'. Do NOT invent intermediate states.
    - The `logs` table dictates the progress percentage dynamically.
    - Always ensure `PRAGMA journal_mode=WAL;` is executed on startup.
    - Use `aiosqlite` with appropriate timeouts to prevent "database is locked" errors.
  </constraint>

  <constraint id="C5" name="Frontend_State">
    Use `zustand` for client-side state management, especially for WebSocket logs. 
    DO NOT use React Context API for high-frequency WebSocket data to prevent rendering bottlenecks.
    Styling must strictly use Tailwind CSS v4 `@theme` directive in `globals.css`. Do not generate `tailwind.config.js`.
  </constraint>
</architectural_constraints>

<tech_stack>
  <backend>
    - Python 3.11+, FastAPI (0.115+), Uvicorn (uvloop)
    - Data Validation: Pydantic v2
    - Database: SQLite with WAL mode (via aiosqlite)
    - Scraper: playwright, playwright-stealth, lxml
    - Mutator: python-lightningcss (PyO3)
    - AI integration: openai, anthropic, tenacity (for backoff)
    - Media: Pillow-SIMD, numpy
  </backend>
  <frontend>
    - Next.js (App Router), TypeScript, React 18+
    - Components: Client Components where interactivity is needed (`use client`).
    - Styling: Tailwind CSS v4
    - State: zustand
    - Animation: framer-motion
  </frontend>
</tech_stack>

<execution_guidelines>
  - Edge Cases: Always implement the edge cases defined in PRD Section 6 (EC-01 to EC-18).
  - Error Handling: All unhandled pipeline exceptions must be caught, logging `level='error'`, and setting `jobs.status='failed'`.
  - Worker concurrency: Use `UPDATE jobs SET status='running' WHERE id=? AND status='pending'` to atomically claim jobs.
</execution_guidelines>